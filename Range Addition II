class Solution {
    public int maxCount(int m, int n, int[][] ops) {
       for(int i = 0; i < ops.length; i++){
           int z = ops[i][0];
           int x = ops[i][1];
           m = Math.min(m, z);
           n = Math.min(n, x);
       } 
       return (m*n);
    }
}
