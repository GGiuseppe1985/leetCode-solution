class Solution {
    public int[] buildArray(int[] nums) {
        int[] hp = new int[nums.length];
        for(int i = 0; i <nums.length; i++){
            hp[i] = nums[nums[i]];
        }
        return hp;
    }
}
